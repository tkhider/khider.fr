# khider.fr

Source for my personal site: static HTML and CSS, no build step, no dependencies.

Pushed to `main`, a GitHub Actions workflow builds the nginx-based Docker image
and publishes it to GHCR, where my homelab picks it up via GitOps.
